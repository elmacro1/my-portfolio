import styles from "./Projects.module.css";

const Projects = () => {
  return (
    <section id="proyectos" className={styles.main_container}>
      <article className={styles.content_container}>
        <h3>Se vienen cosas...</h3>
      </article>
    </section>
  );
};

export default Projects;
